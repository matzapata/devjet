---
title: "Configuring frontend routes"
date: 10/7/2022
summary: "Create a new frontend route with react-router-dom and react"
tags: ["basics","react","react-router-dom"]
category: "basics"
image: "/images/react.png"
pro: false
comingsoon: false
generators: 1
---


```bash:generators
devjet run react new-page --name=PageName --path=page-path
```

All pages components should go under the `pages` folder and exported by default.

1. Create your page component

```tsx:src/pages/MyPage.tsx 
import React from "react";

function MyPage() {
  return null;
}

export default MyPage;
```

2. Register the page in the `App.tsx` file

```tsx:src/App.tsx {7,13}
import React from "react";
import { Route, Routes } from "react-router-dom";

import Home from "pages/Home";
import About from "pages/About";
import NotFound from "pages/NotFound";
import MyPage from "pages/MyPage";

function App() {
  return (
    <Routes>
      <Route path="/about" element={<About />} />
      <Route path="/my-page" element={<MyPage />} />
      <Route path="/" element={<Home />} />
      <Route path="*" element={<NotFound />} />
    </Routes>
  );
}

export default App;
```
 