---
title: "Improve UX of your Next.js App in 3 minutes with Page Loading Indicator"
date: 
summary: "Add a progressbar to indicate the loading state on the navigation between pages"
tags: ["UX"]
category: "receipes"
image: "/images/nextjs.png"
pro: false
comingsoon: false
generators: 0
---


1. Install dependencies

```bash
yarn add nextjs-progressbar
# OR with npm
npm i nextjs-progressbar
```

2. Update your _app.tsx file

```tsx:_app.tsx {8,14}
import React from "react";
import { AppProps } from "next/app";
import { ChakraProvider } from "@chakra-ui/react";
import theme from "../lib/chakraTheme";
import "../styles/global.css";
import { Provider } from "react-redux";
import { store } from "redux/store";
import NextNProgress from "nextjs-progressbar";

const MyApp = ({ Component, pageProps }: AppProps) => {
  return (
    <Provider store={store}>
      <ChakraProvider theme={theme}>
        <NextNProgress />
        <Component {...pageProps} />
      </ChakraProvider>
    </Provider>
  );
};

export default MyApp;
```
