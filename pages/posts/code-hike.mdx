import PostLayout from '../../components/PostLayout'

export const meta = {
  title: "Code hike tutorial",
  description: "Integrate code hike with nextjs to create beutifull code blocks with mdx",
  introduction: "Integrate code hike with nextjs to create beutifull code blocks with mdx",
  cover: "/images/code-hike-cover.png",
  categories: ["UI"],
  tags: ["nextjs", "mdx"]
}

export default ({children}) => <PostLayout meta={meta}>{children}</PostLayout>


# Just testing out [Code Hike](https://codehike.org/)

Some normal `markdown`

## Annotation Example
```js index.js box=1[25:39]
console.log("Some code. I am annotated!")
```

## Focus Example
```js next.config.js focus=1:2,7
const { remarkCodeHike } = require("@code-hike/mdx");
const theme = require("shiki/themes/monokai.json");

const withMDX = require("@next/mdx")({
  extension: /\.mdx?$/,
  options: {
    remarkPlugins: [[remarkCodeHike, { theme }]],
    rehypePlugins: [],
  },
});

module.exports = withMDX({
  pageExtensions: ["ts", "tsx", "js", "jsx", "md", "mdx"],
});
```

<CH.Section>

## Code Links Example

We are looking at the [console.log](focus://console.js#2) function today

<CH.Code>
```js console.js
console.table(["Hello", "World"])
console.log("Hello World")
```
</CH.Code>

</CH.Section>

